<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.islamiczone.org">

    <!-- Core permissions -->
    <!-- 10.2: geocoding প্যাকেজ (lat/lng → জেলার নাম) এবং http (Aladhan API) এর জন্য দরকার -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required for exact-time alarms (oneShotAt exact:true) on Android 12 (API 31)+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Required so the azan notification can wake the screen / launch the
         full-screen alarm activity even over the lock screen (Android 10+). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Lets us ask the user to exempt the app from battery optimization,
         which many OEMs (Xiaomi/Oppo/Vivo/Samsung) otherwise use to kill
         background alarms before they can fire. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- alarm reliability checklist: Xiaomi/Oppo/Vivo/Huawei ইত্যাদির নিজস্ব
         "autostart" সেটিংস স্ক্রিনে সরাসরি নিয়ে যাওয়ার জন্য android_intent_plus
         দিয়ে যেসব প্যাকেজে intent পাঠানো হবে, Android 11 (API 30)+ এ package
         visibility নিয়মের কারণে সেগুলো এখানে আগে থেকে ঘোষণা না করলে সেই
         প্যাকেজ ইনস্টল থাকলেও intent resolve করতে পারবে না — চুপচাপ কিছুই হবে না। -->
    <queries>
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.coloros.oppoguardelf" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.asus.mobilemanager" />
        <package android:name="com.letv.android.letvsafe" />
        <package android:name="com.oneplus.security" />
        <intent>
            <action android:name="android.settings.APPLICATION_DETAILS_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
        </intent>
    </queries>

    <application
        android:label="islamic_zone"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <!--
                turnScreenOn + showWhenLocked let the azan's full-screen
                notification actually wake the device and show the alarm
                screen over the lock screen, the same way an alarm clock or
                incoming call does — this is the core of "সব ফোনেই
                কার্যকরী" reliability, not just a louder sound.
            -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- android_alarm_manager_plus এর জন্য বাধ্যতামূলক তিনটা component।
             এইগুলা manifest এ না থাকলে oneShotAt() দিয়ে schedule করা যায় ঠিকই,
             কিন্তু কাজ পাঠানোর (enqueueWork) সময় JobIntentService component
             manifest এ খুঁজে না পেয়ে exception ছোঁড়ে — এটাই "alarm on করে app এ
             আবার ঢুকলে splash screen এ আটকে থাকা" সমস্যার আসল কারণ, কারণ main()
             এর restoreAlarmsFromPrefs() ধাপে এই একই schedule কল আবার হয় এবং
             uncaught exception এ runApp()/FlutterNativeSplash.remove() পর্যন্ত
             আর পৌঁছায় না। আগের ভুল নামের custom "AlarmBootReceiver" বাদ দিয়ে
             package-এর নিজস্ব সঠিক component নামগুলো ব্যবহার করা হলো। -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false"/>
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false"/>
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
